# Stack

-

```C++
#include <stack>
int main() {
    std::stack<int> myStack;

    // 添加元素到栈中
    myStack.push(1);
    // 输出栈顶元素
	myStack.top();

    // 移除栈顶元素
    myStack.pop();

    // 检查栈是否为空
    myStack.empty()

}

```
